DROP
    DATABASE IF EXISTS shopping_site_data;
CREATE DATABASE shopping_site_data; USE
    shopping_site_data;
DROP TABLE IF EXISTS
    member;
CREATE TABLE member(
    `username` VARCHAR(20) NOT NULL,
    `email` VARCHAR(64) NOT NULL,
    `cellphone` VARCHAR(10) NOT NULL,
    `address` VARCHAR(255) NOT NULL,
    `password` VARCHAR(20) NOT NULL,
    PRIMARY KEY(username)
); INSERT INTO member(
    username,
    email,
    cellphone,
    address,
    PASSWORD
)
VALUES(
    'admin',
    'zed2786328@gmail.com',
    '0920347637',
    'taichung',
    'Ghjj3488'
);
INSERT INTO member(
    username,
    email,
    cellphone,
    address,
    PASSWORD
)
VALUES(
    'karta',
    'qwer@qwer.com',
    '0920555666',
    'tainan',
    'Pass1234'
);
DROP TABLE IF EXISTS
    product_list;
CREATE TABLE product_list(
    `product_id` INT(11) UNSIGNED NOT NULL AUTO_INCREMENT,
    `product_name` VARCHAR(100),
    `product_price` INT(11) UNSIGNED,
    `product_stocks` INT(11) UNSIGNED,
    `product_images` VARCHAR(100),
    `product_description` TEXT,
    PRIMARY KEY(product_id)
); INSERT INTO product_list(
    product_name,
    product_price,
    product_stocks,
    product_images,
    product_description
)
VALUES(
    'BEST Piano',
    '500000',
    '10',
    'piano.png',
    'this is piano.this is piano.this is piano.this is piano.this is piano.this is piano.this is piano.this is piano.this is piano.this is piano.this is piano.this is piano.this is piano.'
);
INSERT INTO product_list(
    product_name,
    product_price,
    product_stocks,
    product_images,
    product_description
)
VALUES(
    'GOOD Computer',
    '40000',
    '9',
    'computer.png',
    'A good computer in taiwan.A good computer in taiwan.A good computer in taiwan.A good computer in taiwan.A good computer in taiwan.'
);
INSERT INTO product_list(
    product_name,
    product_price,
    product_stocks,
    product_images,
    product_description
)
VALUES(
    'Cheap Food',
    '300',
    '65',
    'food.png',
    'Very cheap, but very yummy, great.Very cheap, but very yummy, great.
    Very cheap, but very yummy, great.'
);
DROP TABLE IF EXISTS
    shopping_cart;
CREATE TABLE shopping_cart(
    `quantity` INT(11) UNSIGNED,
    `username` VARCHAR(20) NOT NULL,
    `product_id` INT(11) UNSIGNED NOT NULL,
    FOREIGN KEY(username) REFERENCES member(username),
    FOREIGN KEY(product_id) REFERENCES product_list(product_id)
); INSERT INTO shopping_cart(
    quantity,
    username,
    product_id
)
VALUES('3', 'karta', '1');
INSERT INTO shopping_cart(
    quantity,
    username,
    product_id
)
VALUES('1', 'karta', '3');
INSERT INTO shopping_cart(
    quantity,
    username,
    product_id
)
VALUES('5', 'karta', '2');
DROP TABLE IF EXISTS
    orders;
CREATE TABLE orders(
    `orders_id` INT(11) UNSIGNED NOT NULL AUTO_INCREMENT,
    `date` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP(), `total_price` INT(11) UNSIGNED, `paytype` ENUM('ATM匯款', '線上刷卡', '貨到付款') NOT NULL, `username` VARCHAR(20) NOT NULL, PRIMARY KEY(orders_id), FOREIGN KEY(username) REFERENCES member(username)); INSERT INTO orders(
        total_price,
        paytype,
        username
    )
VALUES('1234560', '貨到付款', 'karta');
DROP TABLE IF EXISTS
    order_detail;
CREATE TABLE order_detail(
    `orders_id` INT(11) UNSIGNED NOT NULL,
    `product_id` INT(11) UNSIGNED NOT NULL,
    `quantity` INT(11) UNSIGNED,
    FOREIGN KEY(orders_id) REFERENCES orders(orders_id),
    FOREIGN KEY(product_id) REFERENCES product_list(product_id)
); INSERT INTO order_detail(
    orders_id,
    product_id,
    quantity
)
VALUES('1', '1', '1');